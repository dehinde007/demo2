
<p>
	<%= link_to (image_tag comment.user.avatar.url(:mini)), comment.user %>
    <%= link_to comment.user.username, comment.user %>
  <%= comment.body %>  <strong><%= time_ago_in_words(comment.created_at) %> ago</strong>
</p>
