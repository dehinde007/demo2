<div class="commentfrmwrap">
    <%= form_for [@micropost, @micropost.comments.build], remote: true do |f| %>
  	<div class="comtfmul">
    <div class="comtfmula"><%= f.text_area :body, class: "font-fam1 commentform", placeholder: "Comment..." %></div>
    <div class="commentformbtn"><%= button_tag "", type: 'submit', class: "btn btn-default glyphicon glyphicon-comment" %></div>
    </div>
    <% end %>
</div>

