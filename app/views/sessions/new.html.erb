<% provide(:title, "Sign in to Hallit") %>

   <div class="container-fluid sess1">  	

   	  <section class="sess1a font-fam1">
      <%= form_for(:session, url: sessions_path) do |f| %>

      <p>
      <%= f.text_field :email, :class =>"sess1a1", placeholder: "Email" %>
      </p>
      
      <p>   
      <%= f.password_field :password, :class =>"sess1a1", placeholder: "Password" %>
      </p>
      </section>
     
      <br />   	
      <div class="font-fam1 blackerror"><%= flash[:alert] %></div>

     <p class="appbutton"><%= f.submit "SIGN IN", class: "font-fam1 btn btn-primary" %></p>
     
    <% end %>
    
   </div>

    <p class="font-fam1 signsess-word">Want to help create a better world? <%= link_to "Sign Up", signup_path, class: "sess1b1" %></p>
