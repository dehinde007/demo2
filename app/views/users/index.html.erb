<%content_for :title, "Find friends and Channels you love"%>
<div class="topsrcbar">
  <div class="topsrcbar1">
   <% if signed_in? %> 		
   <%= render 'shared/search' %>                
   <% end %>
  </div>
</div>

<div class="container usrs">
  <%= render @users %>
</div>

<div class="pager">
   <%= will_paginate @users, :page_links => false %>
</div>

<div><%= render 'layouts/minfooter' %></div>