<% provide(:title, 'Sign up for Hallit') %>

<div class="alert alert-error">
<%= form_for @user, :html => { :multipart => true} do |f| %>
<%= render 'shared/error_messages', object: f.object %>
</div>

<div class="scarcity">
<div class="container signup">
     
     <section class="signupform">
     
     <p class="font-fam">	
      <%= f.label :fullname, :class =>"signuplabel font-fam2" %>
      <%= f.text_field :name, :class =>"signupfield font-fam", placeholder: "Enter fullname" %>
     </p>
     
     <p class="font-fam">
      <%= f.label :username, :class =>"signuplabel font-fam2" %>
      <%= f.text_field :username, :class =>"signupfield font-fam", placeholder: "Enter username" %>
     </p>
     
     <p class="font-fam">
      <%= f.label :email, :class =>"signuplabel font-fam2" %>
      <%= f.text_field :email, :class =>"signupfield font-fam", placeholder: "Enter email" %>
     </p>
     
     <p class="font-fam">
      <%= f.label :password, :class =>"signuplabel font-fam2" %>
      <%= f.password_field :password, :class =>"signupfield font-fam", placeholder: "Enter password" %>
     </p>

    <br />
     <p class="font-fam appbutton"><%= f.submit "Create Account", class: "btn-primary btn-sm" %></p>
    <% end %>
    
    <p class="font-fam">By signing up, you agree to the Terms of Service and Privacy Policy.</p>
   </section>
   <span class="font-fam" style="font-size:11px;"> Version 1.0</span>
</div>
</div>

<p class="misscion sesswarn">Only on Iphone or Android Web Devices!</p>

<br />
<br />

