<%= form_for(current_user.relationships.find_by(followed_id: @user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <span class="sub_but"><%= f.submit "Unsubscribe", class: "btn btn-inverse" %></span>
<% end %></span>