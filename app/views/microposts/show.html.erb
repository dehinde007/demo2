  <p id="notice"><%= notice %></p>
  
  <%= render "shared/microusrn" %>
  
 <div id="post_img">
 <%= image_tag @micropost.photo.url(:normal) %></div>
 
   <% if current_user?(@micropost.user) %>
    <%= link_to "delete", @micropost, method: :delete,
                                     data: { confirm: "You sure?" },
                                     title: @micropost.content %>                              
  <% end %>

 <strong><%= time_ago_in_words(@micropost.created_at) %> ago</strong> 
<p>
  <div style="word-wrap: break-word; width: 400px;"><%= @micropost.content %></div>
</p>
  
<%= pluralize(@micropost.comments.count, "comment") %> 
<%= pluralize(@micropost.likes.count, "like") %> <td><%= link_to 'Show like', like_path %></td>
<br />

<br />
  <%= render @micropost.comments %>

<%= render "comments/form" %>


<% if like = current_user.likes.find_by_micropost_id(@micropost.id) %>
                
<% else %>
  <%= render "likes/form" %>
  <% end %>


<div class="app_footer"><%= render 'layouts/footer' %></div>

