<%content_for :title, "Find friends or Channels you love"%>
<div class="topsrcbar">
  <div class="usindexsearch">
   <% if signed_in? %> 		
   <%= render 'shared/search' %>                
   <% end %>
  </div>
</div>
 <div class="micropostindex"> 
 	
  <% @microposts.each do |micropost| %>
  
  <div class="micropostindex2"> <%= link_to (image_tag micropost.photo.url(:square), class: "img-responsive"), micropost %></div>
  
  <% end %>
  
 </div>

 <div class="pager">
 <%= will_paginate @microposts, :page_links => false %>
 </div>
 
 <div class="footer2"><%= render 'layouts/minfooter' %></div>