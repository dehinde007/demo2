<% if activity.trackable %> 
<span class="font-fam1 blacklink"><%= link_to activity.owner.username, activity.owner if activity.owner %></span> has a new sponsor
<span> <%= time_ago_in_words(activity.created_at) %> ago</span>
<%= link_to (image_tag activity.trackable.photo.url(:square), class: "trackable"), activity.trackable %>
<hr />
<% else %> 
<hr /> 
<% end %>